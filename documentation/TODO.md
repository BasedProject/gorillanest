* currently we are operating in read-only beta mode
* some paths are harcoded
* syncing
* mirroring
* there is no restriction on ssh pushing / pulling; this could be fixed by having a speudo file system for every user with mount+chroot; this makes cooperation easy to implement
* cooperation
* the CSS is so eye-cancer its not even funny
* the ini parser Perl uses, only supports ; comments
* bootstrap script that installs remote dependencies
